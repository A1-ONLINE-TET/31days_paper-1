{
  "name": "a1-online-tet-root",
  "version": "1.0.0",
  "description": "Root package for Cloudflare Pages build",
  "scripts": {
    "build": "cd \"PAPER 1\" && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
